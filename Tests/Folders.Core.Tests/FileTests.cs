namespace Folders.Core.Tests;

[TestClass]
public class FileTests
{
    [TestMethod]
    public void Folder_AddFile_StorageId_MimeType_Empty_Size_Zero()
    {
        // Arrange
        //var folder = Aggregates.Folder.NewFolder("TestFolder");
        //var fileName = "testfile.txt";        
        //// Act
        //var file = folder.AddFile(fileName, Stor);
        //// Assert
        //Assert.IsNotNull(file);
        //Assert.AreEqual(fileName, file.Name);
        //Assert.AreEqual(file.StorageId, Values.StorageId.Empty);
        //Assert.AreEqual(file.MimeType, Values.MimeType.Empty);
        //Assert.IsTrue(file.IsEmpty);
        //Assert.AreEqual(0, file.Size);
    }
}
